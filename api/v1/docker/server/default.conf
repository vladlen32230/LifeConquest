server {
    listen 80;
    server_name lifeconquest.ru;

    root /front;
    index index.html;
    try_files $uri /index.html;

    location /api/v1/ {
        proxy_pass http://backend:8000/;
    }

    location /jwt {
        proxy_pass http://backend:8000/api/v1/jwt;
    }
}
